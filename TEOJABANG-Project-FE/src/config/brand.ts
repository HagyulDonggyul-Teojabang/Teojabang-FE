/**
 * 브랜드 로고 설정
 *
 * 이미지 로고로 교체할 때:
 * 1. 로고 파일을 src/assets/ 에 추가 (예: logo.png)
 * 2. 아래 import 주석 해제
 * 3. LOGO_IMAGE_SRC 를 import 한 변수로 변경
 *
 * import logoImage from '../assets/logo.png'
 * export const LOGO_IMAGE_SRC: string | null = logoImage
 */
export const LOGO_IMAGE_SRC: string | null = null

export const LOGO_ALT = '터잡앙'
export const BRAND_NAME = '터잡앙'
